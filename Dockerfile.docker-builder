FROM docker:24.0.1-cli AS docker-builder

RUN apk add --no-cache bash git ca-certificates curl sudo
RUN adduser -D hived -G users && echo "hived ALL=(ALL) NOPASSWD: SETENV: /bin/cp" >> /etc/sudoers
USER hived
WORKDIR /home/hived
